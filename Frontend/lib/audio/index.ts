/**
 * 2K Music Audio System
 * Universal system-wide audio processing via Equalizer APO
 */

// APO System-Wide Equalizer (Universal audio processing)
export { 
  apoEqualizer, 
  APO_PRESETS, 
  APO_FREQUENCIES,
  type APOBand,
  type APOPreset 
} from './apo-equalizer'

/**
 * Audio System Architecture:
 * 
 * APO Equalizer (apo-equalizer.ts)
 * - Universal system-wide audio processing
 * - Works with ALL applications (YouTube, games, your music app, etc.)
 * - Requires Equalizer APO installation
 * - Professional-grade quality
 * - Single equalizer for everything
 * 
 * Why APO-only?
 * - Eliminates redundancy (one EQ for all audio)
 * - Better user experience (consistent processing)
 * - Professional audio workflow
 * - No need for separate app-specific EQ
 */

// Usage:
// - Use apoEqualizer for ALL audio processing needs
// - Processes your music app AND system audio uniformly
// - One EQ to rule them all!
